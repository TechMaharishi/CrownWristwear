<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Watchara - Luxury Watches</title>
    <link rel="stylesheet" href="/public/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-helvetica_now_display bg-cover bg-center">
    <% if (success.length> 0) { %>
        <div id="flash-message"
            class="absolute top-16 left-1/2 transform -translate-x-1/2 p-3 rounded-md bg-blue-500 shadow-lg animate-bounce">
            <span class="inline-block text-white font-semibold">
                <%= success %>
            </span>
        </div>
        <% } %>
            <div class="min-h-screen flex flex-col bg-gradient-to-r from-yellow-50 to-gray-100">
                <div
                    class="container mx-auto px-4 sm:px-6 lg:px-8 py-12 flex flex-grow flex-col lg:flex-row space-y-6 lg:space-y-0 lg:space-x-6">
                    <div class="w-full lg:w-1/4 flex flex-col items-start bg-white p-6 rounded-lg shadow-lg">
                        <div class="flex flex-col space-y-4 w-full">
                            <a class="block text-yellow-500 font-medium hover:text-yellow-600 transition"
                                href="/owner/admin/allproducts">All Products</a>
                            <a class="block text-yellow-500 font-medium hover:text-yellow-600 transition"
                                href="/owner/admin">Create new product</a>
                            <a class="block text-red-500 font-medium hover:text-red-600 transition"
                                href="/owner/logout">Logout</a>
                        </div>
                    </div>
                    <main class="w-full lg:w-3/4 bg-white p-6 sm:p-10 rounded-lg shadow-lg">
                        <h2 class="text-2xl sm:text-3xl font-extrabold mb-6 sm:mb-8 text-gray-900">Create New Product
                        </h2>
                        <form autocomplete="off" action="/products/create" method="post" enctype="multipart/form-data"
                            class="space-y-6 sm:space-y-8">
                            <div>
                                <h3 class="text-lg sm:text-xl font-semibold mb-4 text-gray-800">Product Details</h3>
                                <div class="mb-4">
                                    <label class="block mb-2 font-medium text-gray-700">Product Image</label>
                                    <input name="image" type="file"
                                        class="w-full py-2 px-4 border rounded-md bg-gray-200 focus:ring-2 focus:ring-yellow-500">
                                </div>
                                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                                    <input name="name" type="text" placeholder="Product Name"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                    <input name="price" type="text" placeholder="Product Price"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                    <input name="discount" type="text" placeholder="Discount Price"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                </div>
                            </div>
                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                                <div>
                                    <label class="block mb-2 font-medium text-gray-700">Category</label>
                                    <select name="category"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                        <option value="Analog Watch">Analog Watch</option>
                                        <option value="Digital Watch">Digital Watch</option>
                                        <option value="Luxury Watch">Luxury Watch</option>
                                        <option value="N/A" selected>N/A</option>
                                    </select>
                                </div>
                            </div>
                            <div>
                                <h3 class="text-lg sm:text-xl font-semibold mb-4 text-gray-800">Panel Details</h3>
                                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                                    <input name="bgColor" type="text" placeholder="Background Color"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                    <input name="panelColor" type="text" placeholder="Panel Color"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                    <input name="textColor" type="text" placeholder="Text Color"
                                        class="border p-3 rounded-md w-full bg-gray-100 focus:ring-2 focus:ring-yellow-500">
                                </div>
                            </div>
                            <input
                                class="px-6 py-3 rounded-full mt-6 bg-yellow-500 text-gray-900 font-bold hover:bg-yellow-600 transition duration-300"
                                type="submit" value="Create New Product">
                        </form>
                    </main>
                </div>
            </div>
            <%- include('./partials/footer') %>